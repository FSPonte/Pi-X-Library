#ifndef _MACROS_HPP_
#define _MACROS_HPP_

#define symbol_name(symbol) #symbol
#define array_size(arr) (sizeof(arr) / sizeof(&arr))

#define TRACEBACK_ENABLED true
#define LOGGER_ENABLED true

#endif // _MACROS_HPP_
